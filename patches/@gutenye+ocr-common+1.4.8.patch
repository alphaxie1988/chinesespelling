diff --git a/node_modules/@gutenye/ocr-common/build/backend/splitIntoLineImages.js b/node_modules/@gutenye/ocr-common/build/backend/splitIntoLineImages.js
index f8bcf2d..7f31b18 100644
--- a/node_modules/@gutenye/ocr-common/build/backend/splitIntoLineImages.js
+++ b/node_modules/@gutenye/ocr-common/build/backend/splitIntoLineImages.js
@@ -12,6 +12,13 @@ export async function splitIntoLineImages(image, sourceImage) {
     const hierarchy = new cv.Mat();
     cv.findContours(src, contours, hierarchy, cv.RETR_LIST, cv.CHAIN_APPROX_SIMPLE);
     for (let i = 0; i < contours.size(); i++) {
+        // A single detected region can occasionally produce a degenerate
+        // contour that this offset/reshape pipeline can't handle — e.g. a
+        // very small or thin region like a pinyin annotation, a stray
+        // correction mark, or text sitting flush against the image border.
+        // Skip just that region instead of failing the whole scan over one
+        // bad shape.
+        try {
         const minSize = 3;
         const cnt = contours.get(i);
         const { points, sside } = getMiniBoxes(cnt);
@@ -48,6 +55,9 @@ export async function splitIntoLineImages(image, sourceImage) {
             box,
             image: c,
         });
+        } catch (err) {
+            console.warn('[ocr] skipping a text region that failed to process:', err);
+        }
     }
     src.delete();
     contours.delete();
diff --git a/node_modules/@gutenye/ocr-common/build/models/Detection.js b/node_modules/@gutenye/ocr-common/build/models/Detection.js
index 417d9bc..a52c6c5 100644
--- a/node_modules/@gutenye/ocr-common/build/models/Detection.js
+++ b/node_modules/@gutenye/ocr-common/build/models/Detection.js
@@ -14,8 +14,11 @@ export class Detection extends ModelBase {
         // Resize image to multiple of 32
         //   - image width and height must be a multiple of 32
         //   - bigger image -> more accurate result, but takes longer time
-        // inputImage = await Image.resize(image, multipleOfBaseSize(image, { maxSize: 960 }))
-        const inputImage = await image.resize(multipleOfBaseSize(image));
+        //   - a real phone photo can be 4000px+ on the long side; running
+        //     detection at that size is extremely slow (tens of seconds) and
+        //     memory-heavy enough to risk crashing on a memory-constrained
+        //     mobile browser, so it's capped before detection runs
+        const inputImage = await image.resize(multipleOfBaseSize(image, { maxSize: 1280 }));
         this.debugImage(inputImage, 'out1-multiple-of-base-size.jpg');
         // Covert image data to model data
         //   - Using `(RGB / 255 - mean) / std` formula
